apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: github-proxy
  name: github-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: github-proxy
  template:
    metadata:
      labels:
        app: github-proxy
    spec:
      containers:
      - args:
        - --cookie-secure=false
        - --provider=github
        - --upstream=http://{{ template "isitup-dashboard.fullname" . }}.kube-system.svc.cluster.local
        - --http-address=0.0.0.0:8080
        - --redirect-url=https://{{ .Values.endpoint }}/oauth2/callback
        - --email-domain=*
        - --github-org={{ .Values.githubProxy.githubOrganization }}
        - --pass-basic-auth={{ .Values.githubProxy.passBasicAuth }}
        - --pass-access-token={{ .Values.githubProxy.passAccessToken }}
        env:
        - name: OAUTH2_PROXY_COOKIE_SECRET
          value: {{ .Values.githubProxy.cookieSecret }}
        - name: OAUTH2_PROXY_CLIENT_ID
          value: {{ .Values.githubProxy.githubAuthClientId }}
        - name: OAUTH2_PROXY_CLIENT_SECRET
          value: {{ .Values.githubProxy.githubAuthSecret }}
        image: {{ .Values.githubProxy.image }}
        name: oauth-proxy
        ports:
        - containerPort: 8080
          protocol: TCP
